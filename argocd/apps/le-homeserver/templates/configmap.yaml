apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "le-homeserver.fullname" . }}-services
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "le-homeserver.labels" . | nindent 4 }}
data:
  services.json: |
    {{- dict "apps" .Values.services.apps | toJson | nindent 4 }}
