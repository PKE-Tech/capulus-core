apiVersion: v1
kind: Service
metadata:
  name: {{ include "le-homeserver.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "le-homeserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "le-homeserver.selectorLabels" . | nindent 4 }}
